## Ip address of the DNS Server used in this system
cat /etc/resov.conf

## command to check the order of dns file and host file 
cat /etc/nsswitsch.conf

## command to bring up network interface
sudo ip link set dev eth0 up

## command ro add default route

sudo ip r default via 172.16.238.1

